# Codex 活跃规则

1. 不重置仓库，不删除已经能编译、能运行或已通过测试的代码。
2. 当前只执行 `CURRENT_TASK.md`；历史版本文档不得覆盖当前 V1.5.0 范围。
3. 禁止在本机搭建或运行开发、构建、模拟器或虚拟设备环境；只复用已连接服务器的 JDK、Gradle、SDK 和缓存。真机测试仅允许复用本机已存在的 ADB 客户端，不得安装新的 Android SDK、ADB、驱动或测试环境。
4. 不主动升级 AGP、Kotlin、Gradle、JDK 或依赖；本包固定版本只在明确构建错误时调整。
5. V1.5.0 Release APK 必须在服务器成功构建并复制到 `deliveries/1.5.0/`，随后由 Codex 使用项目所有者本机已连接的真实 Android 手机完成测试；禁止使用 GitHub Actions、Android Emulator 或任何模拟器。
6. JVM 单测、Lint、PHP 检查、构建和部署只在服务器执行。每次真机测试前必须确认目标 ADB 状态严格为 `device`；设备不可用时停止测试且不得改用模拟器，设备被其他项目占用时保持任务并排队等待。
7. 同一错误无代码或配置变化时最多重试两次；总排障超过 30 分钟必须停止循环并输出单一阻塞报告。
8. 业务页面只能使用 `ui/theme` 和 `ui/components` 中的设计 Token 与公共组件，不得自行硬编码颜色、字号、圆角或随机间距。
9. 不创建新的治理系统、run-loop、attempt、全量合同或重复规则目录；规则修改最多涉及 5 个活跃文档。
10. Fake 数据必须清楚标识；真实阿里云密钥、生产签名和管理员密码不得提交仓库。
11. 当前 APK 使用固定公开 Beta 签名以支持覆盖安装；该签名严禁用于正式生产版。
12. 真机验收必须安装并启动 APK，检查崩溃、ANR 和日志异常；对每个页面执行真实点击、输入、返回、滚动和全部已定义关键业务流程；逐页采集真机截图并与 `ui-reference` 效果图比较布局、字体、颜色、间距、图标和交互状态。
13. 测试证据必须记录设备、APK 版本、测试路径、截图和问题，并保存到对应交付目录。关键功能、视觉验收或任一已定义流程未通过，或证据不完整时，不得报告“通过”或交付 APK。
14. 每次最终交付包括：APK、SHA-256、Commit（如适用）、完成清单、真机测试报告、截图证据和已知问题。
