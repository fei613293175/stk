# V1.4.0 Known Issues And Evidence Limits

- 生产曾存在并发布 `1.5.4 / 10504`，与本任务要求的 V1.3 -> V1.4 顺序冲突。当前已按任务发布 V1.4；已安装 V1.5 的客户端不会自动降级，旧记录/APK 保留供审计和回滚。
- `stk-api` 与 `stk-admin` 已解析但使用自签名 TLS；`stk-static` 与 `stk-download` 尚未解析。当前 Beta 下载和 App Links 由有效的主域名承载。
- 当前签名是仓库内共享 Beta 证书，不应直接视为长期生产签名，见 `SIGNING_DECISION_REQUIRED.md`。
- 本机连接设备最高为 Android 15 / API 35，没有 API 36 真机；未使用模拟器替代。API 36 要求只能作为未覆盖项如实保留。
- Redmi 首次覆盖安装成功但会话已失效，随后设备前台被外部微信流程切换，已停止并释放；Vivo X21 系统安装器以错误 `-2` 拒绝 APK，亦已释放。最终设备验收结论以 `DEVICE_TEST_REPORT.md` 为准。
- SYS-OV 下载中/失败是合同中非阻断的同组件状态；正式交互 `INT-UPD-005/007` 按合同跳转到 SYS-003 下载页，不宣称它们是独立业务下载路径。
- V1.3 -> V1.4 覆盖安装在 Redmi 上由同证书的首版 V1.4 APK验证；最终 APK 只改动用户可见的通用网络失败文案，随后在 V2203A 上覆盖最终首版 V1.4 并完整回归。最终 APK 未在同一台设备上再次从 V1.3 起步，相关证据边界已保留。
