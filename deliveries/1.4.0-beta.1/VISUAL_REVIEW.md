# V1.4.0 Visual Review

参考库存为 23 张 V1.4 PNG；最终状态账本为 23 条 `IMPLEMENTED_VERIFIED`。

最终主验收设备：Vivo `V2203A`，Android 14 / API 34，序列号 `10AC9T177B002T4`，分辨率 `1080 x 2400`。证据目录：`device-test/final-10AC9T177B002T4`。

代表状态已按参考图复核：关于页检查中/最新版/发现更新，启动可选与强制更新，维护与恢复，更新页检查/最新版/可选/强制、字节进度、网络失败、SHA 失败、等待安装、未知来源授权和系统安装器。可选弹层、强制弹层和更新详情页的层级、品牌蓝、警告橙、状态图形、按钮关系、圆角、间距与遮罩结构一致；未观察到文本遮挡、按钮溢出或状态间布局跳动。

客观差异：

- 参考图使用 `1.4.0/1.4.1` 示例，真机使用实际 `1.4.0-beta.1` 与受控 `1.4.0-beta.2` 临时测试记录。
- 参考图下载主机为尚未解析的 `stk-download.zz-yihao.com`，Beta 真机按生产实际显示有效主域名 `stk.zz-yihao.com`。
- 真机状态栏、字体栅格和纵向留白受 Vivo 系统、2400px 高度及实际时间影响。
- 下载进度为真实字节值，不固定在参考图的 60%；真机证据捕获到 1% 及多个下载阶段。

上述差异均来自真实版本、真实网络和设备系统，不改变合同信息层级或交互。SYS-OV 下载中/失败沿用同一更新组件并进入 SYS-003 详情状态，按交互合同审计，不虚构独立下载路径。
