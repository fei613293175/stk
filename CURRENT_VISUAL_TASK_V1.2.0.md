
# 当前任务：V1.2.0 发布、图片上传与审核闭环

## 目标

在保留稳定 V1.1.0 登录、首页、搜索、分类、详情、缓存、签名和构建链路的前提下，实现 V1.2.0 的真实发布纵向闭环，并依据 `ui-reference/detailed/V1.2.0/` 中 53 张效果图完成页面状态一致性。

## 权威顺序

1. 功能、数据、权限、状态机和后台行为：当前仓库版本范围及本包 `contracts/`；
2. 页面结构、颜色、字号、圆角、间距、高度和状态反馈：本包效果图与 `docs/UI_DESIGN_SYSTEM.md`；
3. 效果图内的项目文案、图片、号码、审核原因、日期和数量：仅为 Fixture 数据，不得转化成新增业务。

## 两阶段交付，不得再次把首个 APK卡死

### 阶段 A：快速可测试 APK

先完成最小真实链路：

- 底部“发布”进入真实 `PUB-001`，不再显示阶段占位；
- 标题、简介、分类、联系方式和发布规范可填写及校验；
- 系统 Photo Picker 选择至少一张图片，真实上传接口返回资产 ID；
- `createProject` 真实创建项目，结果只能来自服务端 `pending` 或 `published`；
- 发布结果页可进入“我的发布”；
- “我的发布”能看到刚提交的本人项目并进入详情；
- 后台至少能看到待审核项目并执行通过或驳回，App 刷新后显示真实状态；
- `assembleDebug` 成功且可覆盖安装 V1.1.0。

达到以上条件后立即交付 `1.2.0-beta.1`，不得等待全部上传异常、限额、编辑冲突、分页失败、离线和 53 状态视觉完善。

### 阶段 B：完整状态完善

按 `VISUAL_BATCHES_V1.2.0.csv` 的 B01→B06 完成 53 个状态，并补齐图片排序/封面、单图重试、编辑、下架、驳回重提、软删除、限额、幂等、状态日志和后台配置。每批只运行快速检查、单元/API 测试和 APK 构建，不为每张图单独提交或运行模拟器。

## 不允许做

- 不重置仓库，不删除或重写 V1.0/V1.1 已完成代码；
- 不升级已经成功的 Gradle、AGP、Kotlin、JDK、SDK 或签名；
- 不把 53 张图实现成 53 个独立 Screen；
- 不使用永久 Mock、静态假成功或客户端自行伪造 pending/published；
- 不申请读取整个相册的宽泛权限；优先使用系统 Photo Picker；
- 不只做客户端图片数量/格式校验，服务端必须重新校验 MIME、体积、尺寸、归属和资产状态；
- 不仅通过隐藏按钮控制权限，所有项目写操作必须由服务端验证 UID、状态和版本号；
- 不新增会员、佣金、道具、收藏、评论、私聊、支付或 V1.3.0 以后功能；
- 不新增 API 37、KVM、全量截图或生产部署阻断。

## 实施方法

1. 最多 15 分钟审计当前页面和公共组件，将状态表标记为 `ALREADY_MATCHED / NEEDS_ADJUSTMENT / NOT_IMPLEMENTED / NOT_APPLICABLE`；
2. 复用既有设计 Token、导航、表单、项目卡、Dialog、Bottom Sheet、Snackbar、加载、空状态和错误组件；
3. 同一页面通过 ViewModel/UI State 表达不同状态，不复制页面；
4. 项目状态固定为 `pending / published / rejected / offline / deleted`，操作项由服务端 `owner_actions` 或等价白名单返回；
5. 所有写接口使用幂等键；编辑、下架、重提和删除携带版本号并处理冲突；
6. 网络提交失败时保留非敏感草稿和已上传资产引用，不得要求用户重新输入全部内容；
7. 每完成一批更新 `VISUAL_IMPLEMENTATION_STATUS_V1.2.0.csv`；
8. 同类构建或环境错误最多重试两次，总排障超过 30 分钟立即停止循环并交付最后一个成功 APK与单一阻塞报告。

## 版本号规则

- 首个测试包建议：`versionName=1.2.0-beta.1`、`versionCode=10201`；
- 详细视觉完成包建议：`versionName=1.2.0-beta.2`、`versionCode=10202`；
- 正式版使用更高的 versionCode，例如 `10299`；
- 如果仓库当前 versionCode 已更高，以实际值递增，绝对不能降低，否则无法覆盖安装。

## 完成标准

- 53 个 State ID 均有明确实现状态；
- 发布、上传、结果、编辑、本人详情、我的发布和操作面板使用统一组件与真实 UI State；
- 8 个新增写 API、2 个继承读取 API、6 个数据实体依赖、7 个后台页面依赖和 22 个配置形成完整链路；
- 后台审核、驳回原因和状态日志真实可查；
- 新版可直接覆盖安装上一版且 V1.1.0 浏览功能无回归；
- 交付 APK、SHA-256、Commit、状态完成表、功能清单和 Owner 测试步骤。
