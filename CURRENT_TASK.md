# 当前任务：V1.5.0 全规格收口与服务器交付

## 任务编号

```text
STK-V150
```

## 目标

对照五版本离线开发文档，完成所有可自主解决的 Android 前端、Discuz 后端/API/数据库/后台、版本更新、App Links、自动化检查、线上部署和交付证据。

## 必须完成

- Android 版本 `1.5.0 / 10500`，`minSdk 26`、`compileSdk/targetSdk 36`；
- Android JVM 单测、Release Lint 和 Release APK 构建在服务器通过；
- Discuz PHP 全量语法与认证、项目、路由、完整规格合同测试在服务器通过；
- 短信重发冷却、项目状态机、动态外链白名单、更新下载校验安装流程可用；
- 两个 Discuz 插件的后台模块、数据库升级、发布记录和线上 API 完成部署；
- 更新清单、下载页、APK、SHA-256 与 App Links 公网一致；
- 生成 `deliveries/1.5.0/` 并交付用户真机测试。

## 执行边界

禁止本机环境和 APK 模拟器测试。构建、自动化检查与部署只在已连接服务器执行。正式生产签名、阿里云真实短信凭据和真机人工验收属于外部限制，不阻断其他可解决工作。
