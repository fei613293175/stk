# 后续 RC 模拟器说明

当前包故意不在 `.github/workflows/` 中放置模拟器任务。

Beta 1 真机反馈完成后，才允许新增一个手动 `workflow_dispatch` 的 API 36 关键流程任务，并遵守：

- 不监听普通 Push；
- 最多 30 分钟；
- 只测试 6～8 条关键流程；
- 模拟器基础设施失败不阻断已成功构建的 APK；
- 不恢复 API 37 或 236 状态全量视觉门禁。
