# 商推客五版本总开发计划

## 1. 产品与架构结论

商推客用户端使用 **Kotlin + Jetpack Compose 原生 Android**。App 页面和组件代码全部随 APK 安装，运行时只从 `stk-api.zz-yihao.com` 获取 JSON 和图片数据；内部业务页面禁止 WebView。Discuz! X5.0 继续负责 UID、账号、插件业务、数据库和管理后台，通过标准插件 `stk_auth` 与 `stk_project` 实现，禁止修改 Discuz 核心文件。

用户端只支持 Android 手机竖屏，不开发 PC 用户前端、iOS、平板专用布局或横屏。后台管理继续面向桌面浏览器。

## 2. 五版本量化范围

| APK版本 | Feature | 涉及页面/覆盖层 | 新增视觉状态 | 新增交互 | 新增API | 新增后台页 | 自动测试合同 |
|---|---|---|---|---|---|---|---|
| 1.0.0 | 16 | 17 | 95 | 56 | 18 | 19 | 208 |
| 1.1.0 | 3 | 8 | 29 | 27 | 0 | 2 | 64 |
| 1.2.0 | 5 | 11 | 53 | 45 | 8 | 5 | 119 |
| 1.3.0 | 4 | 13 | 37 | 29 | 5 | 6 | 84 |
| 1.4.0 | 2 | 6 | 22 | 8 | 1 | 3 | 39 |

> 页面数量按本版本首次出现、状态增强或交互增强去重计算。效果图总数与视觉状态数一一对应；236 张独立效果图现已全部生成、通过 SHA 校验并标记为 `APPROVED`。

## 3. 版本纵向闭环

### 1.0.0

完成原生工程、设计系统、三项导航、启动编排、手机号密码/短信登录、注册不发短信、找回密码、安全验证码、Token、协议、基础首页/项目详情、我的基础资料、认证后台、基础项目后台、API/管理员域名和首个 Release APK。后台可创建测试项目，使首页不是无法验证的空壳。发布入口必须显示阶段说明，不能无响应。

### 1.1.0

完成搜索、分类、游标分页、Room 离线优先缓存、图片查看、联系方式、复制/拨号/微信/QQ/浏览器、Custom Tabs 和外链白名单。仍不开放用户发布，发布入口继续显示确定的 V1.2 提示。

### 1.2.0

完成 Photo Picker、多图压缩上传、排序封面、项目发布、编辑、下架、删除、驳回重提、我的发布和完整审核后台。此版删除早期发布占位分支，底部“发布”必须进入真实发布页。

### 1.3.0

完成“我的”完整信息布局：资料卡、会员状态、佣金/任务账户只读余额、道具目录、我的发布、个人资料、客服、设置、关于以及浏览记录/收藏/实名认证的明确占位页。会员、账户和道具只展示，不产生订单、收益或库存。

### 1.4.0

完成可选/强制更新、APK 下载和 SHA/签名校验、App Links、下载落地页、维护开关、生产安全与性能加固、五版本覆盖安装和全量回归，形成最终可运营基线。

## 4. 开发顺序

```text
效果图全部批准
→ V1.0.0 纵向闭环
→ GitHub Actions 验收与桌面交付
→ V1.1.0 覆盖开发与覆盖安装验收
→ V1.2.0
→ V1.3.0
→ V1.4.0 全量终验
```

每个版本内部按“数据/接口与公共组件先行 → 页面纵向实现 → 后台配置/查询/审计 → 自动测试 → CI Release”推进。禁止先做一批假页面再最后统一接后端。

## 5. 完成定义

一个 Feature 只有在其适用的页面、状态、全部交互、API、数据库、后台配置/查询/操作、错误路径、日志和测试均有实现与证据时，才能从 `PLANNED` 改为 `DONE`。仅有页面、仅有接口 200、仅有后台列表或聊天声明都不构成完成。

## 6. 快速执行约束

- 每版一次完整预检，之后只检查受影响范围。
- 每个开发循环必须优先产出代码、测试或可运行结果。
- 相同输入下不得重复跑同一失败命令；先修原因。
- 规划和报告只保留本包规定的必要内容，不创建第二套治理体系。
- 外部 DNS、阿里云或服务器配置缺失时生成 Owner Action 并继续不受影响的代码，不得全项目停摆。
