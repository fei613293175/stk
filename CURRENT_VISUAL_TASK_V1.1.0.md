# 当前任务：V1.1.0 项目浏览与详细视觉完善

## 目标

在保留稳定 V1.0.0 认证、导航、签名、构建链路和本地数据的前提下，实现 V1.1.0 项目浏览纵向闭环，并依据 `ui-reference/detailed/V1.1.0/` 中 45 张效果图完成页面状态一致性。

## 权威顺序

1. 功能、数据和权限：当前仓库的版本范围、API、数据库、后台合同和已经工作的后端；
2. 页面结构、颜色、字号、圆角、间距、高度和状态反馈：本包效果图与 `docs/UI_DESIGN_SYSTEM.md`；
3. 效果图内的项目内容、用户名、头像、号码、浏览量和日期：仅为 Fixture 数据，不得转化成新增业务。

## 两阶段交付，不得再次把首个 APK卡死

### 阶段 A：快速可测试 APK

先完成最小真实链路：

- 首页正常项目流；
- 项目卡进入详情；
- 搜索有结果；
- 分类选择并刷新；
- 图片查看；
- 联系方式复制；
- Fake/真实 API 至少一种模式可运行；
- `assembleDebug` 成功。

达到以上条件后立即交付 `1.1.0-beta.1`，不得等待全部离线、超时、分页失败、外链回退和 45 状态视觉完善。

### 阶段 B：完整状态完善

按 `VISUAL_BATCHES_V1.1.0.csv` 的 B01→B06 完成 45 个状态。每批只运行快速检查、单元测试和 APK 构建，不为每张图单独提交或运行模拟器。

## 不允许做

- 不重置仓库，不删除或重写 V1.0.0 已完成代码；
- 不升级已经成功的 Gradle、AGP、Kotlin、JDK、SDK 或签名；
- 不把 45 张图实现成 45 个独立 Screen；
- 不新增搜索历史、热门推荐、评论、收藏、私聊、保存图片、用户发布或审核功能；
- 不将任意 URL 直接交给 WebView；网址使用白名单、Custom Tabs 和明确回退；
- 不新增 API 37、KVM、全量截图或生产部署阻断；
- 不提前开发 V1.2.0 及以后功能。

## 实施方法

1. 最多 15 分钟审计当前页面和公共组件，将状态表标记为 `ALREADY_MATCHED / NEEDS_ADJUSTMENT / NOT_IMPLEMENTED / NOT_APPLICABLE`；
2. 复用 V1.0.0 统一设计 Token、导航、卡片、Dialog、Snackbar、加载、空状态和错误组件；
3. 同一页面通过 ViewModel/UI State 表达不同状态，不复制页面；
4. 每完成一批更新 `VISUAL_IMPLEMENTATION_STATUS_V1.1.0.csv`；
5. 保留首页滚动位置、分页游标、缓存和覆盖安装数据；
6. 同类构建或环境错误最多重试两次，总排障超过 30 分钟立即停止循环并交付最后一个成功 APK与单一阻塞报告。

## 版本号规则

- 首个测试包建议：`versionName=1.1.0-beta.1`、`versionCode=10101`；
- 详细视觉完成包建议：`versionName=1.1.0-beta.2`、`versionCode=10102`；
- 正式版使用更高的 versionCode，例如 `10199`；
- 如果仓库当前 versionCode 已更高，以实际值递增，绝对不能降低，否则无法覆盖安装。

## 完成标准

- 45 个 State ID 均有明确实现状态；
- 首页、搜索、分类、详情、图片和联系方式使用统一组件与真实 UI State；
- 分页不重复，失败只影响局部；离线有缓存时继续可读；
- 外部链接和外部 App 有白名单、未安装和无效目标回退；
- 新版可直接覆盖安装上一版；
- 交付 APK、SHA-256、Commit、状态完成表、功能清单和 Owner 测试步骤。
